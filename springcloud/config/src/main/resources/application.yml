spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kaibagit/config-repository.git
          searchPaths: springcloud-demo
          username: username
          password: password
server:
  port: 7001